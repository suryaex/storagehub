# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
*.log

# ── Node / Frontend ──
node_modules/
dist/
.vite/
*.local
# tsc -b / vite build artifacts (regenerated on every build)
*.tsbuildinfo
frontend/vite.config.js
frontend/vite.config.d.ts
# lockfile left out — Docker/deploy use `npm install` against package.json
package-lock.json

# ── Runtime storage (do not commit user files) ──
storage/users/
storage/shared/
storage/trash/
storage/temp/
data/

# ── DB ──
*.sqlite3

# ── Editor / OS ──
.idea/
.vscode/
.DS_Store
Thumbs.db
